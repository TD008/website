// src/pages/About.js
import React from 'react';
import { Typography } from '@mui/material';

const About = () => (
  <div>
    <Typography variant="h4">About Page</Typography>
    <Typography variant="body1">Learn more about us on this page.</Typography>
  </div>
);

export default About;
